// 手势与鼠标动作配置
// 文档: https://yalter.github.io/niri/Configuration:-Gestures

gestures {
    // 拖拽窗口到边缘时滚动视图
    // 默认开启，以下为默认值。
    // dnd-edge-view-scroll {
    //     trigger-width 30  // 触发区域宽度 (逻辑像素)
    //     delay-ms 100      // 延迟 (防止误触)
    //     max-speed 1500    // 最大滚动速度 (像素/秒)
    // }

    // 拖拽窗口到边缘时切换工作区 (仅在概览模式下有效)
    // 默认开启，以下为默认值。
    // dnd-edge-workspace-switch {
    //     trigger-height 50
    //     delay-ms 100
    //     max-speed 1500
    // }

    // 屏幕热角配置
    // 默认左上角 (top-left) 触发概览。
    hot-corners {
        // 关闭热角
        // off
        
        // 仅启用特定角落
        // top-left      // 左上 (默认)
        // top-right     // 右上
        // bottom-left   // 左下
        // bottom-right  // 右下
    }
}
